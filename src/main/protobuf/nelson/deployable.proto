syntax = "proto3";

package nelson.api;

option go_package = "deployable";

message Deployable {
  string unit_name = 1;
  oneof kind {
    Docker docker = 2;
  }
}

message Docker {
  string image = 1;
}
